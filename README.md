# PAY BRO Hub v10-clean

Структура:
- `index.html` — главная страница.
- `assets/css/style.css` — чистые стили без накопленных v5/v6/v7/v8/v9-слоёв.
- `assets/js/app.js` — логика поиска, якорей, раскрытия разделов и кнопки наверх.
- `assets/img/logo-pay-bro.jpg` — локальный логотип.
- `assets/data/image-manifest.json` — список изображений инструкции.

Важно:
Картинки шагов сейчас подключены через внешние URL Teletype, потому что исходные файлы изображений не были доступны в рабочем контейнере.
Архитектура уже подготовлена под локализацию: можно скачать картинки и заменить URL в `index.html` на `assets/img/...`.
